user www-data;
worker_processes auto;
pid /run/nginx.pid;

events {
	worker_connections 768;
	# multi_accept on;
}

http {

	access_log /var/log/nginx/access.log;
	error_log /var/log/nginx/error.log;

	gzip on;

	upstream dov-bear {
		least_conn;
		server 206.189.92.248:49166;
		server 206.189.92.248:49167;
		server 206.189.92.248:49165;
	}

	server {
		listen 80;
		location / {
			proxy_pass http://dov-bear;
		}
	}
}